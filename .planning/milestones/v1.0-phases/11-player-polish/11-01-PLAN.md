---
phase: 11-player-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/utils/formatElapsed.ts
  - src/renderer/src/utils/formatElapsed.test.ts
  - src/renderer/src/types/playback.ts
  - src/renderer/src/stores/playbackStore.ts
  - src/renderer/src/components/player/ElapsedTimeMarker.tsx
  - src/renderer/src/components/player/SeparatorCard.tsx
  - src/renderer/src/components/player/PlaybackPlayer.tsx
autonomous: true
requirements: [PLAY-13]

must_haves:
  truths:
    - "Elapsed-time markers appear between consecutive navigation steps within the same session as a centered pill on a thin horizontal rule"
    - "Session separator cards show session duration alongside existing step and message counts"
    - "Section separator cards show section duration alongside existing session and step counts"
    - "No elapsed marker appears for the first step in a session, after separator cards, or when either timestamp is missing"
    - "All timestamp display is gated on presentation.settings.showTimestamps === true"
  artifacts:
    - path: "src/renderer/src/utils/formatElapsed.ts"
      provides: "Pure formatting utility and elapsed computation"
      exports: ["formatElapsed", "computeElapsedMs"]
    - path: "src/renderer/src/components/player/ElapsedTimeMarker.tsx"
      provides: "Pill-on-rule elapsed time divider component"
      exports: ["ElapsedTimeMarker"]
    - path: "src/renderer/src/types/playback.ts"
      provides: "Enriched step types with elapsedMs and duration fields"
      contains: "elapsedMs"
    - path: "src/renderer/src/stores/playbackStore.ts"
      provides: "buildPlaybackSteps enriched with elapsed data and duration stats"
    - path: "src/renderer/src/components/player/SeparatorCard.tsx"
      provides: "Duration display on separator cards"
    - path: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      provides: "ElapsedTimeMarker injected between navigation steps"
  key_links:
    - from: "src/renderer/src/stores/playbackStore.ts"
      to: "src/renderer/src/utils/formatElapsed.ts"
      via: "computeElapsedMs called during buildPlaybackSteps"
      pattern: "computeElapsedMs"
    - from: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      to: "src/renderer/src/components/player/ElapsedTimeMarker.tsx"
      via: "conditional render between navigation steps"
      pattern: "ElapsedTimeMarker"
    - from: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      to: "presentation.settings.showTimestamps"
      via: "gating all timestamp display"
      pattern: "showTimestamps"
---

<objective>
Add elapsed-time markers between Player navigation steps and duration stats on separator cards.

Purpose: Give presenters visible pacing information showing how long each Claude interaction took, enriching the presentation with temporal context.
Output: Elapsed-time pill dividers between steps, session/section duration on separator cards, all gated on showTimestamps setting.
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-player-polish/11-CONTEXT.md
@.planning/phases/11-player-polish/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/renderer/src/types/pipeline.ts:
```typescript
export interface ParsedMessage {
  uuid: string
  parentUuid: string | null
  role: 'user' | 'assistant'
  contentBlocks: ContentBlock[]
  timestamp: string
  requestId: string | undefined
  isMeta: boolean
  isSidechain: boolean
  toolVisibility: ToolVisibility | null
}

export interface NavigationStep {
  index: number
  userMessage: ParsedMessage | null
  assistantMessage: ParsedMessage | null
  followUpMessages: ParsedMessage[]
}
```

From src/renderer/src/types/playback.ts (CURRENT -- will be modified):
```typescript
export interface SectionSeparatorStep {
  type: 'section-separator'
  sectionId: string
  sectionName: string
  sessionCount: number
  totalSteps: number
}

export interface SessionSeparatorStep {
  type: 'session-separator'
  sessionId: string
  sessionName: string
  sectionId: string
  sectionName: string
  stepCount: number
  messageCount: number
}

export interface NavigationPlaybackStep {
  type: 'navigation'
  step: NavigationStep
  sessionId: string
  sectionId: string
}
```

From src/renderer/src/types/presentation.ts:
```typescript
export interface PresentationSettings {
  toolVisibility: ToolCategoryConfig[]
  showTimestamps: boolean
  theme: 'light' | 'dark' | 'system'
}
```

From src/renderer/src/stores/playbackStore.ts:
```typescript
export function buildPlaybackSteps(
  presentation: Presentation,
  sessionsMap: Map<string, StoredSession>
): PlaybackStep[]

export function computeSectionProgress(
  steps: PlaybackStep[],
  currentStepIndex: number,
  sections: PresentationSection[]
): SectionProgressInfo[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Elapsed time utility, type enrichment, and buildPlaybackSteps integration</name>
  <files>
    src/renderer/src/utils/formatElapsed.ts,
    src/renderer/src/utils/formatElapsed.test.ts,
    src/renderer/src/types/playback.ts,
    src/renderer/src/stores/playbackStore.ts
  </files>
  <behavior>
    - formatElapsed(0) returns "<1s"
    - formatElapsed(500) returns "<1s"
    - formatElapsed(-100) returns "<1s" (negative guard)
    - formatElapsed(1000) returns "1s"
    - formatElapsed(12000) returns "12s"
    - formatElapsed(59999) returns "59s"
    - formatElapsed(60000) returns "1m"
    - formatElapsed(150000) returns "2m 30s"
    - formatElapsed(3600000) returns "1h"
    - formatElapsed(3900000) returns "1h 5m"
    - computeElapsedMs(null, "2024-01-01T00:00:00Z") returns null
    - computeElapsedMs("2024-01-01T00:00:00Z", null) returns null
    - computeElapsedMs("", "2024-01-01T00:00:00Z") returns null
    - computeElapsedMs("invalid", "2024-01-01T00:00:00Z") returns null
    - computeElapsedMs("2024-01-01T00:00:00Z", "2024-01-01T00:01:00Z") returns 60000
    - computeElapsedMs("2024-01-01T00:01:00Z", "2024-01-01T00:00:00Z") returns -60000 (negative is valid -- caller handles)
  </behavior>
  <action>
    1. Create `src/renderer/src/utils/formatElapsed.ts` with two pure functions:
       - `formatElapsed(ms: number): string` -- Smart relative format per user decision: <1s for sub-second/negative, Ns for seconds, Nm Ns for minutes (drop 0s), Nh Nm for hours (drop 0m). Use the exact implementation from RESEARCH.md Pattern 1.
       - `computeElapsedMs(prevTimestamp, currTimestamp): number | null` -- Returns null if either input is null, undefined, empty string, or produces NaN. Otherwise returns curr - prev in milliseconds.

    2. Create `src/renderer/src/utils/formatElapsed.test.ts` with exhaustive tests covering all behavior cases above.

    3. Modify `src/renderer/src/types/playback.ts`:
       - Add `elapsedMs: number | null` to `NavigationPlaybackStep` (null = don't show marker)
       - Add `durationMs: number | null` to `SessionSeparatorStep` (session duration)
       - Add `durationMs: number | null` to `SectionSeparatorStep` (section duration)

    4. Modify `src/renderer/src/stores/playbackStore.ts` -- `buildPlaybackSteps`:
       - Import `computeElapsedMs` from `../utils/formatElapsed`
       - Add a helper `getStepTimestamp(step: NavigationStep): string | null` that returns `step.userMessage?.timestamp ?? step.assistantMessage?.timestamp ?? null` (prefer user timestamp, fall back to assistant)
       - Track `prevNavTimestamp: string | null = null` and `prevNavSessionId: string | null = null` across navigation steps
       - For each navigation step: compute `elapsedMs` using `computeElapsedMs(prevNavTimestamp, currTimestamp)` ONLY if `prevNavSessionId === ref.sessionId` (same session). Otherwise set `elapsedMs = null`. Then update tracking vars.
       - For session duration: after building all navSteps for a session, compute duration as `computeElapsedMs(firstNavTimestamp, lastNavTimestamp)` and add as `durationMs` to the SessionSeparatorStep.
       - For section duration: after building all sessions in a section, sum all session durations (only those that are non-null) and add as `durationMs` to the SectionSeparatorStep. If all sessions have null duration, section duration is also null.

    IMPORTANT: Reset `prevNavTimestamp` and `prevNavSessionId` at session boundaries (when starting a new session's nav steps, set prevNavSessionId to null before the loop, so the first step gets elapsedMs = null).
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx vitest run src/renderer/src/utils/formatElapsed.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>formatElapsed and computeElapsedMs pass all tests. PlaybackStep types include elapsedMs/durationMs fields. buildPlaybackSteps enriches steps with computed elapsed data respecting session boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: ElapsedTimeMarker component, SeparatorCard duration, and PlaybackPlayer wiring</name>
  <files>
    src/renderer/src/components/player/ElapsedTimeMarker.tsx,
    src/renderer/src/components/player/SeparatorCard.tsx,
    src/renderer/src/components/player/PlaybackPlayer.tsx
  </files>
  <action>
    1. Create `src/renderer/src/components/player/ElapsedTimeMarker.tsx`:
       - A small presentational component that renders a pill-on-rule timeline marker
       - Props: `{ elapsedMs: number }` -- always receives a positive number (caller handles null gating)
       - Layout: flexbox row with a thin 1px line, a centered pill with `formatElapsed(elapsedMs)`, and another thin 1px line
       - Use module-level style constants (per project convention):
         - Container: `display: flex, alignItems: center, gap: var(--space-2), padding: 0 var(--space-6)`
         - Lines: `flex: 1, height: 1, background: var(--color-border)`
         - Pill: `padding: 2px var(--space-3), fontSize: var(--text-xs), fontFamily: var(--font-mono), fontVariantNumeric: tabular-nums, color: var(--color-text-muted), background: var(--color-bg-tertiary), borderRadius: 999, whiteSpace: nowrap`
       - Use the exact styling from RESEARCH.md code example

    2. Modify `src/renderer/src/components/player/SeparatorCard.tsx`:
       - Add duration display to both separator card types
       - For section-separator: if `step.durationMs` is not null, append formatted duration to the stats line (e.g., "2 sessions, 14 steps, 5m 30s")
       - For session-separator: if `step.durationMs` is not null, append formatted duration to the stats line (e.g., "8 steps, 24 messages, 2m 15s")
       - Import `formatElapsed` from `../../utils/formatElapsed`
       - Only show duration when the value is non-null (graceful degradation for missing timestamps)

    3. Modify `src/renderer/src/components/player/PlaybackPlayer.tsx`:
       - Import `ElapsedTimeMarker` component
       - Read `showTimestamps` from `presentation.settings.showTimestamps`
       - In the step content rendering area, when `currentStep.type === 'navigation'` AND `showTimestamps === true` AND `currentStep.elapsedMs !== null` AND `currentStep.elapsedMs >= 0`:
         - Render `<ElapsedTimeMarker elapsedMs={currentStep.elapsedMs} />` ABOVE the `<StepView>` component
       - The ElapsedTimeMarker should be inside the scroll container div (ref={containerRef}) so it scrolls with the step content
       - Also gate the duration display on SeparatorCard: pass `showTimestamps` to SeparatorCard and only show duration when enabled. Two approaches (choose the simpler):
         - Option A: Pass `showTimestamps` as a prop to SeparatorCard, let it conditionally render duration
         - Option B: Compute `durationMs` as null when `showTimestamps` is false (simpler -- SeparatorCard already handles null)
       - Use Option B: when creating the step object for rendering, if `showTimestamps` is false, treat `durationMs` as null. This can be done inline: `step.durationMs != null && showTimestamps ? step.durationMs : null`. Actually, even simpler: just pass showTimestamps to SeparatorCard as a prop and gate inside SeparatorCard.
       - Use Option A: add `showTimestamps?: boolean` prop to SeparatorCard. In SeparatorCard, only render the duration portion when `showTimestamps && step.durationMs != null`.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>ElapsedTimeMarker renders as pill-on-rule between navigation steps. SeparatorCard shows duration stats. All timestamp display gated on showTimestamps setting. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors from new fields or imports)
2. `npx vitest run src/renderer/src/utils/formatElapsed.test.ts` passes (all edge cases)
3. Build the app: `npm run build` completes without errors
4. Visual check: Open a .promptplay file with showTimestamps enabled -- elapsed markers appear between steps as pill dividers, separator cards show duration
5. Visual check: Open a .promptplay file with showTimestamps disabled -- no elapsed markers or duration stats visible
</verification>

<success_criteria>
- Elapsed-time markers appear between consecutive same-session navigation steps as "--- 2m 30s ---" style pill dividers
- Session and section separator cards show duration alongside existing stats
- First step in a session has no elapsed marker
- Missing timestamps produce no marker (no placeholder, no error)
- Cross-session boundaries produce no marker (inter-session gaps not displayed)
- All display gated on showTimestamps setting
- formatElapsed utility fully tested with edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/11-player-polish/11-01-SUMMARY.md`
</output>
