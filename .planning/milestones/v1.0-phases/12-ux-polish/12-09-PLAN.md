---
phase: 12-ux-polish
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/styles/message.css
  - src/renderer/src/routes/Builder.tsx
autonomous: true
requirements: [UX-POLISH-02]
gap_closure: true

must_haves:
  truths:
    - "Builder live preview in light theme shows readable dark text on light background"
    - "Elapsed time markers and gaps between messages use light theme colors when light theme is selected"
    - "Message text in YOU and CLAUDE bubbles is visible and readable in light theme"
  artifacts:
    - path: "src/renderer/src/styles/message.css"
      provides: "Explicit color declaration on .message-view"
      contains: "color: var(--color-text-primary)"
    - path: "src/renderer/src/routes/Builder.tsx"
      provides: "data-theme on outer preview container"
      contains: "data-theme"
  key_links:
    - from: "src/renderer/src/styles/message.css"
      to: "theme.css"
      via: "CSS variable resolution"
      pattern: "color-text-primary"
    - from: "src/renderer/src/routes/Builder.tsx"
      to: "theme.css"
      via: "data-theme attribute scoping"
      pattern: "data-theme.*resolvedTheme"
---

<objective>
Fix light theme rendering in Builder live preview: (1) message text invisible due to missing color declaration, (2) elapsed time markers and separator gaps staying dark because the outer container sits outside the data-theme scope.

Purpose: UAT blocker -- users cannot read message text when light theme is selected in Builder preview.
Output: Readable light-theme preview with correct text colors and themed separators/markers.
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ux-polish/12-06-SUMMARY.md

<interfaces>
<!-- From theme.css: light theme scoped via data-theme attribute -->
[data-theme="light"] sets:
  --color-text-primary: #0f172a
  --color-text-secondary: #475569
  --color-text-muted: #94a3b8
  --color-bg-primary: #ffffff
  --color-bg-secondary: #f8fafc
  --color-bg-tertiary: #f1f5f9

<!-- From message.css: .message-view lacks color declaration (line 14-20) -->
.message-view {
  max-width: 900px;
  margin: 0 auto;
  font-size: var(--text-preview-base);
  line-height: 1.6;
  overflow-x: hidden;
  /* NO color declaration -- inherits from body which resolves dark theme */
}

<!-- From Builder.tsx: preview structure (lines 482-542) -->
<!-- OUTER container (outside data-theme scope): -->
<div className="flex flex-col" style={{ backgroundColor: 'var(--color-bg-secondary)', ... }}>
  <!-- Header bar -->
  <div>Live Preview / Close</div>
  <!-- INNER data-theme wrapper (only this gets light theme): -->
  <div data-theme={resolvedTheme} style={{ flex: 1, overflowY: 'auto' }}>
    <MessageList messages={filteredMessages} showTimestamps={...} />
  </div>
</div>

<!-- The outer div resolves --color-bg-secondary in dark context.
     The inner div has data-theme but the outer does not. -->

<!-- resolvedTheme computed at line 263-268: -->
const resolvedTheme = useMemo(() => {
  if (!activePresentation?.settings) return undefined
  const t = activePresentation.settings.theme
  if (t === 'system') return isDarkMode ? 'dark' : 'light'
  return t
}, [activePresentation?.settings?.theme, isDarkMode])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add color declaration to .message-view and move data-theme to outer preview container</name>
  <files>src/renderer/src/styles/message.css, src/renderer/src/routes/Builder.tsx</files>
  <action>
Two fixes for the light theme blocker:

**Fix 1 -- message.css:**
Add `color: var(--color-text-primary);` to the `.message-view` rule (around line 18). This ensures message text color resolves from the nearest ancestor with data-theme, instead of inheriting from body which is always in dark mode context. Place it after the `line-height` declaration.

**Fix 2 -- Builder.tsx:**
Move the `data-theme={resolvedTheme}` attribute from the inner scrollable div (currently at line 537) UP to the outer preview container div (currently at line 482). This ensures the entire preview area -- including the "Live Preview" header bar, the background behind separators, and the elapsed time markers -- all resolve CSS variables in the correct theme context.

Specifically in Builder.tsx, find the assembly view preview section (around line 481-543):
- On the outer `<div className="flex flex-col"` that has `backgroundColor: 'var(--color-bg-secondary)'`, add `data-theme={resolvedTheme}`.
- On the inner `<div data-theme={resolvedTheme}`, remove the `data-theme` attribute (it's now on the parent).

The inner div should keep its existing styles (`flex: 1, overflowY: 'auto', minHeight: 0`) unchanged. Only the data-theme attribute moves up one level.

Do NOT change any other file or any other part of Builder.tsx. The browse view preview panel does not need changes (it doesn't have theme override).
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - .message-view has explicit `color: var(--color-text-primary)` declaration
    - data-theme attribute is on the outer preview container div, not the inner scrollable div
    - TypeScript compiles without errors
    - In Builder with light theme selected: message text is dark on light background, elapsed time markers use light theme colors, separator gaps have light background
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. In Builder, create/open a presentation, set theme to "light", click a session in outline to preview:
   - YOU bubble text should be dark (#0f172a) on light tertiary background (#f1f5f9)
   - CLAUDE bubble text should be dark (#0f172a) on light primary background (#ffffff)
   - Elapsed time markers between messages should use light theme muted color and tertiary background
   - The "Live Preview" header area and gaps between messages should have light background, not dark
</verification>

<success_criteria>
Light theme preview is fully readable: text is dark on light backgrounds, all UI elements (markers, separators, header) render in light theme colors. No dark theme bleed-through in the preview area.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ux-polish/12-09-SUMMARY.md`
</output>
