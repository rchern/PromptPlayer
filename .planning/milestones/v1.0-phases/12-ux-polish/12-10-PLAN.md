---
phase: 12-ux-polish
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/player/PlaybackPlayer.tsx
autonomous: true
requirements: [UX-POLISH-03]
gap_closure: true

must_haves:
  truths:
    - "Segmented progress bar does not overlap step content in the Player"
    - "Last line of step content is fully visible above the progress bar"
    - "Scrolling to the bottom of a step reveals all content without occlusion"
  artifacts:
    - path: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      provides: "paddingBottom on scrollable container to clear progress bar"
      contains: "paddingBottom"
  key_links:
    - from: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      to: "src/renderer/src/components/player/SegmentedProgress.tsx"
      via: "paddingBottom reserves space for absolute-positioned progress bar"
      pattern: "paddingBottom"
---

<objective>
Add paddingBottom to the Player's scrollable content area so step content does not scroll underneath the absolute-positioned segmented progress bar.

Purpose: UAT minor issue -- section separator text and step content render behind the progress indicator at the bottom of the viewport.
Output: Content scrolls with clearance above the progress bar.
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From SegmentedProgress.tsx: absolute positioning (lines 6-14) -->
const containerStyle: React.CSSProperties = {
  position: 'absolute',
  bottom: 'var(--space-3)',    /* 0.75rem = 12px */
  left: 'var(--space-4)',
  right: 'var(--space-4)',
  zIndex: 10,
  userSelect: 'none',
  pointerEvents: 'none'
}
/* Total height: text row (~20px) + marginBottom (--space-1 = 4px) + bar (4px) = ~28px */
/* Plus bottom offset (--space-3 = 12px) = ~40px total from viewport bottom */

<!-- From PlaybackPlayer.tsx: scrollable container (lines 166-198) -->
<div
  ref={containerRef}
  style={{
    height: '100%',
    overflowY: 'auto',
    opacity: visible ? 1 : 0,
    transition: 'opacity 75ms ease-in-out'
    /* NO paddingBottom -- content scrolls under progress bar */
  }}
>
  {/* Step content renders here */}
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paddingBottom to scrollable container to clear progress bar</name>
  <files>src/renderer/src/components/player/PlaybackPlayer.tsx</files>
  <action>
In PlaybackPlayer.tsx, find the scrollable container div with `ref={containerRef}` (around line 167). Add `paddingBottom: 'var(--space-12)'` (3rem = 48px) to its style object. This provides enough clearance for the progress bar (text row + bar + bottom offset = ~40px) plus a small visual buffer.

The change is a single property addition:
```
style={{
  height: '100%',
  overflowY: 'auto',
  paddingBottom: 'var(--space-12)',   // <-- ADD THIS LINE
  opacity: visible ? 1 : 0,
  transition: 'opacity 75ms ease-in-out'
}}
```

Do NOT change SegmentedProgress.tsx positioning (absolute is by design for the floating progress bar). Do NOT change any other component.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Scrollable container has `paddingBottom: 'var(--space-12)'`
    - TypeScript compiles without errors
    - In the Player, scrolling to the bottom of any step shows all content above the progress bar with no overlap
    - Section separator text is fully visible and not occluded by the progress indicator
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. In the Player, load a presentation and navigate to a step with enough content to scroll. Scroll to the bottom -- the last line of content should be fully visible above the progress bar. No text should render behind or be hidden by the segmented progress indicator.
3. Navigate to a section separator card -- its text should be fully visible without progress bar overlap.
</verification>

<success_criteria>
All step content and separator text in the Player is fully visible with adequate clearance above the segmented progress bar. No visual overlap at any scroll position.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ux-polish/12-10-SUMMARY.md`
</output>
