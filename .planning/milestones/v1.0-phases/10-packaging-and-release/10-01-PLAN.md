---
phase: 10-packaging-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron-builder.yml
  - build/icon.ico
  - package.json
  - dev-app-update.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running 'npm run build:win' produces a Windows installer (.exe) in the dist/ output"
    - "electron-builder.yml configures NSIS assisted installer with per-user install"
    - "File association for .promptplay is declared in build config"
    - "Auto-update publish metadata points to GitHub Releases"
  artifacts:
    - path: "electron-builder.yml"
      provides: "Complete electron-builder configuration"
      contains: "nsis"
    - path: "build/icon.ico"
      provides: "Placeholder app icon for installer and file association"
    - path: "package.json"
      provides: "Build scripts for Windows packaging"
      contains: "build:win"
    - path: "dev-app-update.yml"
      provides: "Local dev auto-update test config"
      contains: "provider"
  key_links:
    - from: "electron-builder.yml"
      to: "package.json"
      via: "build scripts reference electron-builder"
      pattern: "electron-builder"
    - from: "electron-builder.yml"
      to: "build/icon.ico"
      via: "icon path in win config"
      pattern: "build/icon.ico"
---

<objective>
Set up electron-builder configuration, build scripts, and placeholder icon for Windows packaging.

Purpose: Establish the build infrastructure so the app can be packaged as a distributable Windows installer with NSIS, file association config, and auto-update metadata.
Output: electron-builder.yml, build/icon.ico, updated package.json with build scripts, dev-app-update.yml
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-packaging-and-release/10-RESEARCH.md
@.planning/phases/10-packaging-and-release/10-CONTEXT.md
@package.json
@electron.vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install electron-builder and electron-updater dependencies</name>
  <files>package.json</files>
  <action>
Install dependencies:
- `npm install -D electron-builder` (devDependency -- build tool only)
- `npm install electron-updater` (runtime dependency -- runs in packaged app)

Add build scripts to package.json scripts section:
- `"build:win": "npm run build && electron-builder --win --config"` (full build + package)
- `"build:unpackaged": "npm run build && electron-builder --win --dir"` (build without installer, for quick testing)

Do NOT modify any existing scripts. Only add the two new ones.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && node -e "const p = require('./package.json'); const ok = p.scripts['build:win'] && p.scripts['build:unpackaged'] && p.devDependencies['electron-builder'] && p.dependencies['electron-updater']; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>package.json has electron-builder as devDependency, electron-updater as dependency, and build:win + build:unpackaged scripts</done>
</task>

<task type="auto">
  <name>Task 2: Create electron-builder.yml, placeholder icon, and dev-app-update.yml</name>
  <files>electron-builder.yml, build/icon.ico, dev-app-update.yml</files>
  <action>
1. Create `electron-builder.yml` in project root with the following configuration:
   - `appId`: `com.promptplayer.app`
   - `productName`: `PromptPlayer`
   - `directories.buildResources`: `build`
   - `files` exclusions: `.vscode/`, `src/`, `.planning/`, `test-data/`, `.agents/`, `.claude/`, electron.vite.config files, eslint/prettier configs, env files, tsconfig files, tsbuildinfo, package-lock.json
   - `asarUnpack`: `resources/**`
   - `win.executableName`: `PromptPlayer`
   - `win.icon`: `build/icon.ico`
   - NSIS config: `oneClick: false` (assisted installer per user decision), `perMachine: false` (per-user install), `allowToChangeInstallationDirectory: false`, `createDesktopShortcut: true`, `createStartMenuShortcut: true`, `shortcutName: PromptPlayer`, `artifactName: ${name}-${version}-setup.${ext}`, `uninstallDisplayName: ${productName}`
   - `fileAssociations`: ext `promptplay`, name `PromptPlayPresentation`, description `PromptPlayer Presentation File`, icon `build/icon.ico`
   - `publish`: provider `github`, owner `OWNER_PLACEHOLDER` (to be set when repo is pushed), repo `PromptPlayer`, `releaseType: draft` (user wants draft releases)
   - `npmRebuild: false`

2. Create `build/` directory and generate a placeholder `build/icon.ico`:
   - Use a simple programmatic approach: create a minimal valid .ico file (16x16 + 32x32 + 48x48 + 256x256 BMP-format ICO). Since the user explicitly said to use Electron default/placeholder icon for v1, generate a minimal solid-color icon using a Node.js script or copy Electron's default icon.
   - Simplest approach: use `npx electron-icon-builder` or manually create a minimal .ico. If tooling is unavailable, create a simple script that generates a valid .ico with a solid teal (#0d9488) square to match the app's accent color.

3. Create `dev-app-update.yml` in project root for local auto-update testing:
   ```yaml
   provider: generic
   url: http://localhost:8080
   ```
   This allows testing the auto-update flow locally without hitting GitHub.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && node -e "const fs = require('fs'); const yml = fs.existsSync('electron-builder.yml'); const ico = fs.existsSync('build/icon.ico'); const dev = fs.existsSync('dev-app-update.yml'); const ymlContent = fs.readFileSync('electron-builder.yml','utf8'); const hasNsis = ymlContent.includes('oneClick: false'); const hasFileAssoc = ymlContent.includes('promptplay'); console.log({yml,ico,dev,hasNsis,hasFileAssoc}); const ok = yml && ico && dev && hasNsis && hasFileAssoc; process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>electron-builder.yml exists with NSIS assisted installer config, .promptplay file association, and GitHub publish provider. build/icon.ico exists as a valid icon file. dev-app-update.yml exists for local testing.</done>
</task>

</tasks>

<verification>
- `electron-builder.yml` contains NSIS config with `oneClick: false` and `perMachine: false`
- `electron-builder.yml` contains `fileAssociations` with `ext: promptplay`
- `electron-builder.yml` contains `publish.provider: github`
- `build/icon.ico` exists and is a valid ICO file
- `package.json` has `build:win` and `build:unpackaged` scripts
- `electron-builder` is in devDependencies
- `electron-updater` is in dependencies
</verification>

<success_criteria>
Build infrastructure is in place: running `npm run build:win` should invoke electron-vite build followed by electron-builder to produce a Windows NSIS installer. The config declares .promptplay file association and GitHub Releases auto-update.
</success_criteria>

<output>
After completion, create `.planning/phases/10-packaging-and-release/10-01-SUMMARY.md`
</output>
