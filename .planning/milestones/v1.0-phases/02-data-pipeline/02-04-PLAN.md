---
phase: 02-data-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/renderer/src/routes/Builder.tsx
  - src/renderer/src/components/builder/SessionList.tsx
  - src/renderer/src/components/builder/SessionCard.tsx
autonomous: false

must_haves:
  truths:
    - "Builder mode shows a list of sessions discovered from ~/.claude/projects/"
    - "Sessions are grouped by project folder and sorted by date within each group"
    - "Each session shows project name, date, and first user message snippet"
    - "Unparseable files appear with a visible error indicator"
    - "User can click 'Browse other location' to discover from a custom directory"
    - "Discovery runs automatically when Builder is first visited"
    - "Selecting a session triggers full parse and shows message count and basic info"
  artifacts:
    - path: "src/renderer/src/routes/Builder.tsx"
      provides: "Builder mode root with session discovery integration"
      contains: "useSessionStore"
    - path: "src/renderer/src/components/builder/SessionList.tsx"
      provides: "Grouped session list with project headers"
      contains: "projectFolder"
    - path: "src/renderer/src/components/builder/SessionCard.tsx"
      provides: "Individual session card with metadata display"
      contains: "SessionMetadata"
  key_links:
    - from: "src/renderer/src/routes/Builder.tsx"
      to: "src/renderer/src/stores/sessionStore.ts"
      via: "useSessionStore hook"
      pattern: "useSessionStore"
    - from: "src/renderer/src/components/builder/SessionList.tsx"
      to: "SessionMetadata"
      via: "props typed with pipeline types"
      pattern: "SessionMetadata"
    - from: "src/renderer/src/routes/Builder.tsx"
      to: "window.electronAPI"
      via: "Zustand store actions trigger IPC"
      pattern: "discover"
---

<objective>
Replace the Builder mode placeholder with a session browse list that displays discovered sessions, proving the entire Phase 2 pipeline works end-to-end.

Purpose: This is the verification UI for the data pipeline. Without it, we cannot confirm that discovery, parsing, stitching, and classification actually work with real JSONL files. It also establishes the Builder mode foundation that Phase 5 will expand.

Output: Builder mode shows a grouped session list with project names, dates, snippets, and error badges. User can browse alternate directories. Selecting a session triggers full parse.
</objective>

<execution_context>
@C:\Users\Rebecca\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Rebecca\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-03-SUMMARY.md
@src/renderer/src/routes/Builder.tsx
@src/renderer/src/stores/sessionStore.ts
@src/renderer/src/stores/appStore.ts
@src/renderer/src/types/pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session browse list in Builder mode</name>
  <files>src/renderer/src/routes/Builder.tsx, src/renderer/src/components/builder/SessionList.tsx, src/renderer/src/components/builder/SessionCard.tsx</files>
  <action>
**SessionCard.tsx** -- Create `src/renderer/src/components/builder/SessionCard.tsx`:
- Accepts `SessionMetadata` as props plus an `onSelect: (session: SessionMetadata) => void` callback
- Displays:
  - First user message snippet (truncated to ~100 chars with ellipsis, or "No preview available" if null)
  - Date formatted from `firstTimestamp` (use `new Date(timestamp).toLocaleDateString()` + `toLocaleTimeString()` -- keep it simple, no date library)
  - Message count if available (at Claude's discretion per CONTEXT.md -- include it, it adds useful context)
  - Error badge if `parseError` is set: a red/orange indicator with "Error" text. Per locked decision: unparseable files appear with an error badge.
- Style with Tailwind classes and CSS variables (match existing component patterns from Phase 1):
  - Card with border, subtle background, rounded corners
  - Hover effect (slight background change or border highlight)
  - Clickable (cursor-pointer, onClick calls onSelect)
  - Error state: distinct visual treatment (red-tinted border or background)

**SessionList.tsx** -- Create `src/renderer/src/components/builder/SessionList.tsx`:
- Accepts `sessions: SessionMetadata[]`, `onSelectSession: (session: SessionMetadata) => void`, `isLoading: boolean`
- Groups sessions by `projectFolder` (use a simple reduce/Map to group)
- Sorts groups alphabetically by project folder name
- Within each group, sorts sessions by `firstTimestamp` descending (newest first). Per locked decision: "sorted by date within each group"
- Renders:
  - Project group headers showing the project folder name (e.g., "D--Code-PromptPlayer")
  - SessionCard for each session within the group
  - Loading state: a simple "Discovering sessions..." text with a subtle animation (at Claude's discretion per CONTEXT.md)
  - Empty state: "No sessions found" message with suggestion to check the path or use "Browse other location"

**Builder.tsx** -- Replace the placeholder content entirely:
- Import and use `useSessionStore` from stores
- On component mount (useEffect with empty deps), call `store.discover()` to auto-discover sessions. Per locked decision: "Auto-discover from ~/.claude/projects/ on app launch and on manual refresh"
- Layout:
  - Header area with title "Session Browser" and two action buttons:
    - "Refresh" button that calls `store.discover()` again
    - "Browse Other Location" button that calls `store.browseAndDiscover()`. Per locked decision: "'Browse other location' fallback for files from other machines or backups"
  - Main area: `<SessionList>` with discovered sessions
  - If a session is selected (activeSession in store), show a basic detail panel below or beside the list showing:
    - Session ID
    - Message count from the stitched session (actual count, not metadata approximation)
    - Orphaned message count and sidechain count from StitchedSession
    - A simple "Parsing..." indicator while `isParsing` is true
    - Parse error display if `parseError` is set
- When user clicks a SessionCard, call `store.parseSession(session.filePath, session.sessionId)` to trigger full parse
- Handle `discoveryError` by showing an error message at the top of the session list area
- Style: Use Tailwind utilities + CSS variables. Keep it functional, not polished -- this is Phase 2 verification UI that Phase 5 will redesign. Clean and readable is enough.
  </action>
  <verify>
Run `npx tsc --noEmit` from the project root. No type errors.
Run `npm run dev` to start the app. Navigate to Builder mode. Verify:
1. Sessions are discovered and listed (grouped by project)
2. Each session shows date and snippet
3. "Browse Other Location" opens native directory picker
4. Clicking a session triggers parsing (shows loading then details)
  </verify>
  <done>Builder mode displays discovered sessions grouped by project with metadata. Users can browse alternate locations. Selecting a session triggers full parse. Error states are visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 2 data pipeline: JSONL discovery, parsing, assistant turn reassembly, parentUuid chain stitching, tool call classification, IPC bridge, Zustand state management, and Builder session browse UI. The entire pipeline runs against real Claude Code JSONL files from ~/.claude/projects/.</what-built>
  <how-to-verify>
1. Run `npm run dev` in the project root to start the app
2. Click "Builder" on the home screen
3. Verify sessions are automatically discovered and listed:
   - Sessions should be grouped by project folder (e.g., "D--Code-PromptPlayer")
   - Each session should show a date and first message snippet
   - Sessions should be sorted by date within each group (newest first)
4. Click on any session to trigger full parse:
   - Should show "Parsing..." briefly
   - Then show message count, orphan count, sidechain count
   - Message count should be reasonable (not hundreds of thousands -- that would indicate lines aren't being reassembled)
5. Click "Browse Other Location" and select any directory:
   - Should open a native Windows directory picker
   - If you select a directory with no JSONL files, should show "No sessions found"
   - If you cancel, nothing should change
6. Click "Refresh" to re-scan
7. If you have any corrupted or unusual JSONL files, verify they show an error badge rather than crashing
  </how-to-verify>
  <resume-signal>Type "approved" if the pipeline works correctly, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
Full pipeline verification:
```bash
npx tsc --noEmit
npm run dev
```

End-to-end test:
1. App launches
2. Builder mode shows session list from ~/.claude/projects/
3. Sessions grouped by project, sorted by date
4. Session selection triggers full parse
5. Parse results show reasonable message counts
6. Browse Other Location opens native dialog
7. Errors display as badges, not crashes
</verification>

<success_criteria>
- Builder mode replaces placeholder with functional session browser
- Auto-discovery runs on mount
- Sessions grouped by project folder, sorted by date (locked decisions)
- Session cards show project name, date, first message snippet (locked decisions)
- Error badges on unparseable files (locked decision)
- "Browse other location" opens native directory picker (locked decision)
- Full parse on session select shows stitched message count
- Human verification confirms real JSONL files parse correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-04-SUMMARY.md`
</output>
