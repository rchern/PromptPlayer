---
phase: 09-specialized-tool-call-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/message/AskUserQuestionBlock.tsx
  - src/renderer/src/components/message/cleanUserText.ts
  - src/renderer/src/components/message/ToolCallBlock.tsx
  - src/renderer/src/components/player/StepView.tsx
autonomous: true
requirements:
  - PLAY-07

must_haves:
  truths:
    - "AskUserQuestion tool calls display a header label tag above the question text"
    - "AskUserQuestion options show labels by default with expandable descriptions"
    - "AskUserQuestion blocks show which option(s) the user selected, highlighted within the question block"
    - "Multi-question AskUserQuestion blocks show each question with its corresponding selected answer"
    - "The same answer does not appear twice (once in the question block and once as a followUp message)"
  artifacts:
    - path: "src/renderer/src/components/message/AskUserQuestionBlock.tsx"
      provides: "Rich AskUserQuestion renderer with header, expandable options, selected answer"
      min_lines: 80
    - path: "src/renderer/src/components/message/cleanUserText.ts"
      provides: "Enhanced parseUserAnswer returning all question-answer pairs"
      contains: "parseUserAnswerPairs"
  key_links:
    - from: "src/renderer/src/components/message/ToolCallBlock.tsx"
      to: "AskUserQuestionBlock"
      via: "dispatch by tool name"
      pattern: "name === 'AskUserQuestion'"
    - from: "src/renderer/src/components/message/AskUserQuestionBlock.tsx"
      to: "cleanUserText.ts"
      via: "parseUserAnswerPairs import"
      pattern: "import.*parseUserAnswerPairs"
    - from: "src/renderer/src/components/player/StepView.tsx"
      to: "followUpMessages filtering"
      via: "suppress tool_result for AskUserQuestion when answer displayed in question block"
      pattern: "tool_use_id"
---

<objective>
Build a presentation-quality AskUserQuestion renderer that shows header labels, expandable option descriptions, and integrated selected-answer highlighting.

Purpose: Replace the basic AskUserQuestion rendering (question text + flat option chips) with a rich display that shows the header label as a tag, option labels with expandable descriptions, and which option(s) the user actually selected — all in one unified block. This fulfills PLAY-07's requirement for "interactive-looking prompts showing the question, options, and the user's selection."

Output: AskUserQuestionBlock.tsx component, enhanced parseUserAnswer utility, dispatch wiring, and followUp message suppression to prevent answer duplication.
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-specialized-tool-call-display/09-RESEARCH.md

@src/renderer/src/components/message/ToolCallBlock.tsx
@src/renderer/src/components/message/cleanUserText.ts
@src/renderer/src/components/message/ThinkingBlock.tsx
@src/renderer/src/components/message/MessageBubble.tsx
@src/renderer/src/components/message/ContentBlockRenderer.tsx
@src/renderer/src/components/player/StepView.tsx
@src/renderer/src/utils/messageFiltering.ts
@src/renderer/src/types/pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AskUserQuestionBlock component and enhance parseUserAnswer</name>
  <files>
    src/renderer/src/components/message/AskUserQuestionBlock.tsx
    src/renderer/src/components/message/cleanUserText.ts
  </files>
  <action>
**1. Enhance `parseUserAnswer` in `cleanUserText.ts`:**

Add a new exported function `parseUserAnswerPairs` that extracts ALL question-answer pairs from the tool_result content string, not just the first one. The format is: `"Q1"="A1", "Q2"="A2"`. Use a global regex match to capture all pairs. Return type: `{ pairs: Array<{ question: string; answer: string }> } | null`. Keep the existing `parseUserAnswer` function unchanged for backward compatibility.

Also handle the "user notes" suffix: content may end with `user notes: free text here. You can now continue...`. If user notes exist, include them as an extra field on the return type.

```typescript
export function parseUserAnswerPairs(text: string): {
  pairs: Array<{ question: string; answer: string }>
  userNotes: string | null
} | null {
  // Match all "question"="answer" pairs with global regex
  // Handle user notes suffix
}
```

**2. Create `AskUserQuestionBlock.tsx`:**

A new React function component that renders a rich AskUserQuestion display. Props:

```typescript
interface AskUserQuestionBlockProps {
  input: Record<string, unknown>
  toolUseId: string
  answerText: string | null  // Paired tool_result content, or null if no answer yet
}
```

Implementation details:

- Parse `input.questions` array (see RESEARCH.md for schema: `{ question, header?, options[], multiSelect }`)
- For each question in the array:
  - If `header` exists, render it as a small tag/label above the question (use accent color background, small font, uppercase, rounded). Per locked decision: "Show the header label as a tag/label above the question text."
  - Render the question text prominently
  - Render options as a vertical list. Each option shows its `label` by default. If the option has a `description`, show a ChevronRight expand/collapse toggle. Use the ThinkingBlock expand/collapse pattern: local `useState<Set<number>>` for expanded indices, ChevronRight with `rotate(90deg)` transition. Per locked decision: "Option descriptions should be expandable — show labels by default, expand for full descriptions."
  - If `answerText` is provided, parse it with `parseUserAnswerPairs()` to get each question's selected answer. Highlight the matching option(s) with accent color border + background. For multi-select, multiple options can be highlighted. Per locked decision: "Must show which option(s) the user selected."
  - If the answer doesn't match any option label (free-text "other" answer), display it below the options as a quoted text block

- Style constants should be extracted as module-level objects (per Pitfall 4). Use inline styles with CSS custom properties consistent with the existing codebase.

- Defensive parsing: handle empty questions array, missing fields, malformed input. Fall back to returning `null` (caller will use generic ToolCallBlock).

- Multi-question layout: stack questions vertically in one block (each question separated by a subtle divider line). This is Claude's discretion — stacking is the simplest and clearest approach.

- Use lucide-react `MessageCircleQuestion` or `CircleHelp` icon next to the header tag for visual identity. Keep it subtle.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify AskUserQuestionBlock.tsx exists and exports the component. Verify parseUserAnswerPairs is exported from cleanUserText.ts.</manual>
  </verify>
  <done>AskUserQuestionBlock renders header labels, expandable options, and highlighted selected answers. parseUserAnswerPairs extracts all question-answer pairs from multi-question tool_result content.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatch and suppress duplicate followUp answers</name>
  <files>
    src/renderer/src/components/message/ToolCallBlock.tsx
    src/renderer/src/components/player/StepView.tsx
  </files>
  <action>
**1. Update `ToolCallBlock.tsx` dispatch:**

- Import `AskUserQuestionBlock` from `./AskUserQuestionBlock`
- Add a new optional prop to ToolCallBlockProps: `answerText?: string | null` — the paired tool_result content for this tool_use_id. Default to `null` if not provided.
- Replace the existing AskUserQuestion branch (lines 67-105, the `extractAskOptions` + inline JSX) with a dispatch to AskUserQuestionBlock:

```typescript
if (name === 'AskUserQuestion') {
  return <AskUserQuestionBlock input={input} toolUseId={toolUseId} answerText={answerText ?? null} />
}
```

- The existing `extractAskOptions` function can be removed (AskUserQuestionBlock handles its own parsing).
- Keep the generic fallback for all other tools unchanged.

**2. Thread `answerText` from StepView through MessageBubble/ContentBlockRenderer:**

The answer text lives in `step.followUpMessages` as a tool_result block whose `tool_use_id` matches the AskUserQuestion's tool_use block id. To get it to ToolCallBlock:

- In `ContentBlockRenderer.tsx`, add an optional `answerText` prop and pass it through to ToolCallBlock when block.type is 'tool_use'.
- In `MessageBubble.tsx`, for assistant messages, build a lookup from tool_use_id to answer text using the followUpMessages. Pass a new optional `followUpMessages` prop to MessageBubble. For each tool_result in followUpMessages, normalize the content and map `tool_use_id -> content string`. Then pass `answerText={followUpAnswerMap.get(block.id)}` to ContentBlockRenderer for tool_use blocks.
- In `StepView.tsx`, pass `followUpMessages={step.followUpMessages}` to the assistant MessageBubble.

**3. Suppress duplicate followUp rendering in `StepView.tsx`:**

After the assistant MessageBubble now shows answers inline in AskUserQuestionBlock, the followUpMessages that are AskUserQuestion tool_result answers should NOT also render below as separate MessageBubbles. Filter them out:

- In the followUpMessages rendering section of StepView.tsx, filter out followUp messages whose tool_result blocks ALL have `tool_use_id`s that map to an AskUserQuestion tool_use (check `toolUseMap` — if `toolUseMap.get(tool_use_id)?.name === 'AskUserQuestion'`).
- Other followUp messages (tool rejections, non-AskUserQuestion tool results) continue to render normally.

This prevents the Pitfall 1 duplication where the same answer appears both in the question block and as a followUp chip.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open a .promptplay file containing AskUserQuestion steps. Verify: (1) header label appears as tag above question, (2) option labels are visible, descriptions expand on click, (3) selected answer is highlighted, (4) answer does NOT appear twice.</manual>
  </verify>
  <done>ToolCallBlock dispatches AskUserQuestion to the specialized renderer. Answer text flows from followUpMessages through the component chain. FollowUp messages for AskUserQuestion answers are suppressed when the answer is displayed inline in the question block.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. App builds successfully: `npm run build`
3. Visual verification: Open PromptPlayerInitialPhases.promptplay in Player mode. Navigate to a step containing AskUserQuestion. Confirm:
   - Header label tag is visible above the question
   - Option labels display by default; clicking an option with a description expands to show the description
   - The selected answer is highlighted with accent styling
   - No duplicate answer display (answer appears only in the question block, not also as a followUp chip below)
4. Multi-question verification: If a multi-question AskUserQuestion exists in test data, confirm each question shows its own answer correctly paired
</verification>

<success_criteria>
- AskUserQuestion tool calls render with header label, expandable options, and selected answer highlighting
- Multi-question inputs show each question with its own paired answer
- Answer is not duplicated between the question block and the followUp messages
- TypeScript compiles cleanly
- Existing rendering of other tool types is unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/09-specialized-tool-call-display/09-01-SUMMARY.md`
</output>
