---
phase: 08-player-multi-session-playback
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/renderer/src/components/player/PlaybackPlayer.tsx
  - src/renderer/src/components/player/PresentationOverview.tsx
  - src/renderer/src/components/player/SeparatorCard.tsx
  - src/renderer/src/hooks/usePlaybackKeyboardNavigation.ts
  - src/renderer/src/routes/Player.tsx
autonomous: true
requirements:
  - PLAY-01
  - PLAY-10

must_haves:
  truths:
    - "Opening a .promptplay file in Player shows a presentation overview screen (title slide) with presentation name, step count, estimated duration, and section names"
    - "Pressing spacebar or right arrow from the overview advances to the first section separator"
    - "Navigation crosses session boundaries seamlessly with separator cards appearing between sessions"
    - "Section separator cards show section name, session count, and total steps as a prominent chapter title"
    - "Session separator cards show session name, section breadcrumb, step count, and message count"
    - "Forward then backward always returns to the exact same step (separator cards included)"
    - "Keyboard S key toggles the sidebar placeholder area (full sidebar in Plan 03)"
  artifacts:
    - path: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      provides: "Multi-session playback wrapper component"
      min_lines: 60
    - path: "src/renderer/src/components/player/PresentationOverview.tsx"
      provides: "Title slide component showing presentation metadata"
      min_lines: 40
    - path: "src/renderer/src/components/player/SeparatorCard.tsx"
      provides: "Section and session separator card rendering"
      min_lines: 50
    - path: "src/renderer/src/hooks/usePlaybackKeyboardNavigation.ts"
      provides: "Keyboard navigation hook for playback mode (extends single-session pattern with sidebar toggle)"
      min_lines: 30
    - path: "src/renderer/src/routes/Player.tsx"
      provides: "Modified Player route that dispatches to PlaybackPlayer when presentation is loaded"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/routes/Player.tsx"
      to: "src/renderer/src/stores/playbackStore.ts"
      via: "usePlaybackStore selector for presentation !== null check"
      pattern: "usePlaybackStore.*presentation"
    - from: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      to: "src/renderer/src/stores/playbackStore.ts"
      via: "usePlaybackStore selectors for steps, currentStepIndex, navigation actions"
      pattern: "usePlaybackStore"
    - from: "src/renderer/src/routes/Player.tsx"
      to: "src/renderer/src/components/player/PlaybackPlayer.tsx"
      via: "Conditional render when playbackStore has a presentation loaded"
      pattern: "PlaybackPlayer"
---

<objective>
Build the core playback UI: the PlaybackPlayer wrapper component, PresentationOverview (title slide), SeparatorCard (section and session transitions), keyboard navigation for playback mode, and the modified Player route that dispatches between single-session and multi-session modes.

Purpose: This plan delivers the user-facing experience of opening a .promptplay file and stepping through a multi-session presentation. The overview screen acts as a title slide. Separator cards give presenters natural pause points at section and session boundaries. The Player route automatically detects whether a presentation is loaded and renders the appropriate mode.

Output: Five files (3 new components, 1 new hook, 1 modified route).
</objective>

<execution_context>
@C:/Users/Rebecca/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rebecca/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-player-multi-session-playback/08-RESEARCH.md
@.planning/phases/08-player-multi-session-playback/08-01-SUMMARY.md

@src/renderer/src/routes/Player.tsx
@src/renderer/src/components/player/StepView.tsx
@src/renderer/src/components/player/NavigationControls.tsx
@src/renderer/src/hooks/useKeyboardNavigation.ts
@src/renderer/src/types/playback.ts
@src/renderer/src/stores/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PresentationOverview, SeparatorCard, and usePlaybackKeyboardNavigation</name>
  <files>
    src/renderer/src/components/player/PresentationOverview.tsx
    src/renderer/src/components/player/SeparatorCard.tsx
    src/renderer/src/hooks/usePlaybackKeyboardNavigation.ts
  </files>
  <action>
**PresentationOverview.tsx:**
Create a centered, full-height component that displays:
- Presentation name as a large heading (var(--text-3xl), fontWeight 700)
- "{totalSteps} steps across {sectionCount} sections" in secondary text
- "Estimated duration: {duration}" in muted text (compute duration as totalSteps * 30 seconds, format as "~X minutes")
- Section names listed vertically in secondary text color
- "Press any key to begin" hint at the bottom in muted small text

Props: `presentation: Presentation`, `totalSteps: number`.
Use .presentation-mode class for 20px base font (per 03-03 decision). Center vertically and horizontally with flexbox.

**SeparatorCard.tsx:**
Create a component that renders both section separators and session separators, distinguishing them visually:

Props: `step: PlaybackStep` (the step must be type 'section-separator' or 'session-separator').

Section separator layout (larger, centered — "chapter title page"):
- Section name as centered heading (var(--text-2xl), fontWeight 700)
- "{sessionCount} sessions, {totalSteps} steps" in secondary text below
- Subtle horizontal rule or divider above and below
- Use accent color for the section name

Session separator layout (smaller, left-aligned — "topic transition"):
- Section name as a small breadcrumb above (muted, var(--text-sm))
- Session name as heading (var(--text-xl), fontWeight 600)
- "{stepCount} steps, {messageCount} messages" in secondary text
- Visually lighter than section separators (no accent color, smaller text)

Both use .presentation-mode class for font sizing. Full-height centering with flexbox so the card fills the viewport like a slide.

**usePlaybackKeyboardNavigation.ts:**
Create a hook that extends the keyboard navigation pattern from useKeyboardNavigation.ts but reads from `usePlaybackStore` instead of `useNavigationStore`. Bind:
- ArrowRight / Space → nextStep
- ArrowLeft → prevStep
- Home → goToFirst
- End → goToLast
- 's' (no modifier) OR Ctrl+B → toggleSidebar
- Skip INPUT/TEXTAREA elements (same guard as existing hook)

This hook replaces useKeyboardNavigation when in playback mode. The existing hook remains for single-session Builder preview.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify all three files exist with correct exports</manual>
  </verify>
  <done>PresentationOverview renders title slide with presentation metadata. SeparatorCard renders both section and session separator variants with visual hierarchy. usePlaybackKeyboardNavigation provides arrow key + sidebar toggle bindings against the playback store.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlaybackPlayer and modify Player route for multi-session dispatch</name>
  <files>
    src/renderer/src/components/player/PlaybackPlayer.tsx
    src/renderer/src/routes/Player.tsx
  </files>
  <action>
**PlaybackPlayer.tsx:**
Create the multi-session playback wrapper component. This is the main rendering surface when a presentation is loaded.

Structure:
```
<div style={{ display: 'flex', height: '100%' }}>
  {/* Sidebar placeholder — will be filled in Plan 03. For now, reserve the flex slot but render nothing. */}
  <aside style={{
    width: sidebarOpen ? 280 : 0,
    transition: 'width 200ms ease',
    overflow: 'hidden',
    flexShrink: 0
  }}>
    <div style={{ width: 280, padding: 'var(--space-4)' }}>
      {/* Plan 03 will add SectionSidebar here */}
    </div>
  </aside>

  {/* Main content area */}
  <div style={{ flex: 1, position: 'relative', overflow: 'hidden' }}>
    {/* Step content with fade transition */}
    <div ref={containerRef} style={{ height: '100%', overflowY: 'auto', opacity, transition }}>
      {/* Render based on current step type */}
      {currentStep.type === 'overview' && <PresentationOverview ... />}
      {currentStep.type === 'section-separator' && <SeparatorCard step={currentStep} />}
      {currentStep.type === 'session-separator' && <SeparatorCard step={currentStep} />}
      {currentStep.type === 'navigation' && <StepView ... />}
    </div>

    {/* Navigation arrows — same NavigationControls as single-session */}
    <NavigationControls ... />

    {/* Progress placeholder — Plan 03 will replace ProgressIndicator with SegmentedProgress */}
    <ProgressIndicator currentStep={currentStepIndex} totalSteps={steps.length} />
  </div>
</div>
```

Use the same fade transition pattern as the existing Player.tsx (75ms opacity toggle on step change, scroll to top).

Build the toolUseMap spanning ALL sessions in the presentation (iterate sessions Map, merge all tool_use blocks). Pass it to StepView for rejection display.

Read state from usePlaybackStore: steps, currentStepIndex, expandedSteps, sidebarOpen, presentation, and navigation actions.

Call `usePlaybackKeyboardNavigation()` for keyboard bindings.

**Player.tsx (modify):**
Update the Player route to dispatch between single-session and multi-session mode:

1. Add `const presentation = usePlaybackStore((s) => s.presentation)` selector.
2. If `presentation !== null`, render `<PlaybackPlayer />` and return early.
3. Otherwise, fall through to the existing single-session logic (activeSession from sessionStore → navigationStore → StepView).

This keeps the existing single-session path completely unchanged. The only addition is the early-return check at the top.

Also add a load trigger: when the Player route mounts and has no presentation loaded, check if there's an import mechanism. For now, the load trigger will come from the Home screen or Builder (future — this plan just wires the rendering). The playback store's `loadPresentation` will be called externally (e.g., from a "Play" button or route parameter — to be wired when integrating with the Home screen in a later task or gap closure).

For initial development/testing: Add a temporary useEffect in Player.tsx that calls `importPresentation()` IPC when the player route mounts with no active session and no presentation loaded, then loads the result into the playback store. This allows testing the full flow. The effect should:
- Call `window.electronAPI.importPresentation()` to trigger the file open dialog
- If result is not null, call `playbackStore.loadPresentation(result.presentation, result.sessions)`
- Guard so it only triggers once (use a ref flag)

NOTE: This auto-import behavior is a development aid. The final flow will be triggered from the Home screen's "Player" card or a file association. But it provides an immediate way to test the full pipeline.
  </action>
  <verify>
    <automated>cd D:/Code/PromptPlayer && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Launch app, navigate to Player, select a .promptplay file. Verify overview screen appears, then arrow key navigation steps through separator cards and message steps.</manual>
  </verify>
  <done>PlaybackPlayer renders the correct component for each step type (overview, separators, navigation steps). Player.tsx dispatches to PlaybackPlayer when a presentation is loaded in the playback store. Navigation arrows and keyboard shortcuts work across session boundaries. Fade transitions and scroll-to-top work on step changes.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with zero errors
2. Player route renders PlaybackPlayer when playbackStore has a presentation loaded
3. Player route falls back to single-session mode when no presentation is loaded
4. PresentationOverview displays presentation name, step count, estimated duration, section names
5. SeparatorCard renders section separators (large, centered, accent color) and session separators (smaller, left-aligned, with breadcrumb)
6. Forward/backward navigation steps through all step types including separator cards
7. Keyboard S key toggles sidebar width (0 ↔ 280px) via CSS transition
8. Fade transition (75ms opacity) triggers on every step change
</verification>

<success_criteria>
- `npx tsc --noEmit` passes
- Opening a .promptplay file in Player shows the overview screen
- Arrow keys / spacebar navigate through overview → section separator → session separator → message steps → next session separator → etc.
- Back arrow returns to separator cards (not skipped)
- Sidebar placeholder opens/closes with S key
- All existing single-session Player behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-player-multi-session-playback/08-02-SUMMARY.md`
</output>
