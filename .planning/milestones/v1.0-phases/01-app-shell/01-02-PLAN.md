---
phase: 01-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/routes/Home.tsx
  - src/renderer/src/routes/Builder.tsx
  - src/renderer/src/routes/Player.tsx
  - src/renderer/src/components/Titlebar/Titlebar.tsx
  - src/renderer/src/components/Titlebar/WindowControls.tsx
  - src/renderer/src/components/home/ModeCard.tsx
  - src/renderer/src/components/home/RecentFiles.tsx
autonomous: false

must_haves:
  truths:
    - "User sees a home screen with two side-by-side cards for Builder and Player modes"
    - "Each card shows an icon, title, and one-line description"
    - "A recent files section appears below the cards (empty state for now)"
    - "Clicking Builder card navigates to Builder mode (placeholder page)"
    - "Clicking Player card navigates to Player mode (placeholder page)"
    - "Custom titlebar shows app name and branded window controls"
    - "Home button in titlebar returns user to home from Builder/Player"
    - "App looks clean and minimal with teal accent color"
  artifacts:
    - path: "src/renderer/src/routes/Home.tsx"
      provides: "Home screen with ModeCard pair and RecentFiles"
      min_lines: 20
    - path: "src/renderer/src/components/home/ModeCard.tsx"
      provides: "Reusable card component for Builder/Player selection"
      contains: "ModeCard"
    - path: "src/renderer/src/components/home/RecentFiles.tsx"
      provides: "Recent .promptplay files list with empty state"
      contains: "RecentFiles"
    - path: "src/renderer/src/components/Titlebar/Titlebar.tsx"
      provides: "Custom titlebar with app name, home nav, and window controls"
      contains: "Titlebar"
    - path: "src/renderer/src/components/Titlebar/WindowControls.tsx"
      provides: "Minimize/maximize/close button group"
      contains: "WindowControls"
    - path: "src/renderer/src/routes/Builder.tsx"
      provides: "Builder mode placeholder"
      contains: "Builder"
    - path: "src/renderer/src/routes/Player.tsx"
      provides: "Player mode placeholder"
      contains: "Player"
  key_links:
    - from: "src/renderer/src/routes/Home.tsx"
      to: "src/renderer/src/components/home/ModeCard.tsx"
      via: "renders two ModeCard instances"
      pattern: "ModeCard"
    - from: "src/renderer/src/components/home/ModeCard.tsx"
      to: "react-router navigate"
      via: "click handler navigates to /builder or /player"
      pattern: "useNavigate|navigate"
    - from: "src/renderer/src/components/Titlebar/Titlebar.tsx"
      to: "src/renderer/src/components/Titlebar/WindowControls.tsx"
      via: "renders WindowControls"
      pattern: "WindowControls"
    - from: "src/renderer/src/components/Titlebar/Titlebar.tsx"
      to: "react-router navigate"
      via: "home button navigates to /"
      pattern: "navigate.*/"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/routes/Home.tsx"
      via: "HashRouter route for /"
      pattern: "path.*/"
---

<objective>
Build the home screen with two side-by-side mode selection cards (Builder and Player), a recent files list, and a branded custom titlebar with working window controls and home navigation.

Purpose: This is the user-facing entry point to the entire application. The home screen establishes the visual identity (clean, minimal, teal accent) and the core navigation pattern (full-screen mode replacement). It fulfills both SHELL-01 and SHELL-02 requirements.

Output: A polished home screen with clickable Builder/Player cards that navigate to placeholder mode screens, a branded titlebar with min/max/close controls and home button, and a recent files section (empty state for Phase 1).
</objective>

<execution_context>
@C:\Users\Rebecca\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Rebecca\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell/01-RESEARCH.md
@.planning/phases/01-app-shell/01-CONTEXT.md
@.planning/phases/01-app-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the custom titlebar with branded styling and window controls</name>
  <files>
    src/renderer/src/components/Titlebar/Titlebar.tsx
    src/renderer/src/components/Titlebar/WindowControls.tsx
    src/renderer/src/App.tsx
  </files>
  <action>
Create the custom titlebar components and integrate them into App.tsx. Use Tailwind CSS utility classes for layout, referencing CSS custom properties from theme.css for colors.

**src/renderer/src/components/Titlebar/WindowControls.tsx:**
- Three buttons: minimize, maximize/restore, close
- Use Lucide icons: `Minus` for minimize, `Square` for maximize, `Copy` for restore (when maximized)
- Track isMaximized state via `window.electronAPI.isMaximized()` on mount and `window.electronAPI.onMaximizeChange()` listener
- On click: call `window.electronAPI.minimize()`, `.maximize()`, `.close()`
- Styling: buttons are small (roughly 46x32px each), inline-flex centered icons
- Minimize and maximize buttons: background transparent, hover shows `var(--color-titlebar-btn-hover)`
- Close button: hover shows `var(--color-titlebar-close-hover)` (red) with white icon
- All buttons must have `-webkit-app-region: no-drag` (applied via parent or directly)

**src/renderer/src/components/Titlebar/Titlebar.tsx:**
- Full-width bar, height `var(--titlebar-height)` (36px), fixed at top
- `-webkit-app-region: drag` on the titlebar container (enables window dragging)
- Left section: App brand text "PromptPlayer" in semi-bold, small size. If NOT on home route, show a home icon button (Lucide `Home` icon, 14px) that navigates to `/` via react-router `useNavigate`. Use `useLocation` to detect current route.
- Center section: empty (acts as draggable region for window movement)
- Right section: `<WindowControls />` component
- Background: `var(--color-titlebar-bg)`, text: `var(--color-titlebar-text)`
- Border bottom: 1px solid `var(--color-border-subtle)` for subtle separation

**src/renderer/src/App.tsx** -- Update:
- Import Titlebar component
- Replace the inline placeholder titlebar with the real `<Titlebar />` component
- The layout structure should be:
  ```
  <div className="app-container"> (flex column, h-screen)
    <Titlebar />
    <main className="app-content"> (flex-1, overflow-auto)
      <RouterProvider router={router} />
    </main>
  </div>
  ```
- NOTE: The router needs to be structured so Titlebar has access to router context. Use a layout approach: either render Titlebar inside a layout route, OR use `createHashRouter` with a root layout element that includes both Titlebar and Outlet. The cleanest approach for this app:
  - Define a RootLayout component that renders `<Titlebar />` + `<Outlet />`
  - Router config: `{ path: '/', element: <RootLayout />, children: [{ index: true, element: <Home /> }, { path: 'builder', element: <Builder /> }, { path: 'player', element: <Player /> }] }`
  - This gives Titlebar access to `useNavigate` and `useLocation` from react-router

Keep all route components as simple placeholders for now (Task 2 builds the real Home). Builder and Player just render a centered heading like "Builder Mode" or "Player Mode" with a note that it's a placeholder.
  </action>
  <verify>
Run `npm run dev`:
1. Custom titlebar appears at the top with "PromptPlayer" brand text and min/max/close buttons
2. Titlebar is draggable (can move window by dragging the center area)
3. Min/max/close buttons are NOT draggable (clicking them works, not drag)
4. Minimize button minimizes the window
5. Maximize button maximizes; icon changes to restore icon; clicking again restores
6. Close button closes the app
7. Home button does NOT appear when on home route
8. Navigate to #/builder -- home button appears in titlebar, clicking it returns to home
  </verify>
  <done>
Custom titlebar renders with branded "PromptPlayer" text, working window controls (minimize, maximize/restore, close), and a home navigation button that appears only when not on the home route. Titlebar is draggable while buttons remain clickable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the home screen with mode cards and recent files</name>
  <files>
    src/renderer/src/routes/Home.tsx
    src/renderer/src/routes/Builder.tsx
    src/renderer/src/routes/Player.tsx
    src/renderer/src/components/home/ModeCard.tsx
    src/renderer/src/components/home/RecentFiles.tsx
  </files>
  <action>
Create the home screen components. This is the main visual deliverable of Phase 1. Use Tailwind CSS utilities with CSS custom properties from theme.css.

**src/renderer/src/components/home/ModeCard.tsx:**
- Props: `icon` (React node or Lucide icon component), `title` (string), `description` (string), `to` (route path string)
- Renders a large clickable card that navigates to `to` on click (use `useNavigate` from react-router)
- Layout: vertical stack -- icon at top (large, ~48px, teal accent color), title below (text-2xl, semi-bold), description below (text-base, muted color)
- Card styling:
  - Background: `var(--color-bg-elevated)`
  - Border: 1px solid `var(--color-border)`
  - Border radius: `var(--radius-lg)` (0.75rem)
  - Shadow: `var(--shadow-md)`
  - Padding: generous (var(--space-8) to var(--space-12))
  - Hover state: border color transitions to `var(--color-accent)`, shadow increases to `var(--shadow-lg)`, subtle scale transform (scale(1.02)) -- keep it restrained per user's "content is the star" directive
  - Cursor: pointer
  - Transition: all properties, ~200ms ease
- The card should feel substantial but not flashy. Think: "developer tool, not consumer app"
- IMPORTANT: Cards are side-by-side (horizontal), per locked decision. Use flex-row with gap.

**src/renderer/src/components/home/RecentFiles.tsx:**
- Reads `recentFiles` from Zustand appStore
- If empty (which it will be in Phase 1): show a subtle empty state message like "No recent files" in muted text, centered
- If files exist (future phases): render a list with file name, last opened date
- Styling: below the cards, full width of the card container area, compact list style
- For Phase 1, this is purely structural -- the empty state is the expected output

**src/renderer/src/routes/Home.tsx:**
- Centered content area within the main content zone (below titlebar)
- Use flex column layout, centered both horizontally and vertically (or with generous top padding for a grounded feel -- not exactly vertically centered, more like 30% from top)
- Top section: optionally a brief welcome heading like "PromptPlayer" or just skip it if the titlebar brand is sufficient -- use judgment, keep it minimal
- Card section: two `<ModeCard>` components side by side in a flex row with gap
  - Builder card: icon = Lucide `Wrench` icon, title = "Builder", description = "Import and curate Claude Code sessions into presentations"
  - Player card: icon = Lucide `Play` icon, title = "Player", description = "Step through presentations in a clean, focused view"
- Below cards: `<RecentFiles />` component
- The overall feel should be clean, spacious, minimal. Lots of whitespace. The two cards are the visual center of attention.

**src/renderer/src/routes/Builder.tsx:**
- Simple placeholder: centered content with "Builder Mode" heading and a subtitle like "Coming in Phase 5" in muted text
- Clean and minimal, consistent with the app's aesthetic
- This is a full-screen replacement of the home content (per discretionary decision: full-screen mode replacement)

**src/renderer/src/routes/Player.tsx:**
- Simple placeholder: centered content with "Player Mode" heading and a subtitle like "Coming in Phase 4" in muted text
- Same styling approach as Builder placeholder

**Visual quality bar:** This should look like a real app, not a prototype. The CSS custom properties handle all theming. Use Tailwind for layout (flex, gap, padding, centering). Use CSS variables for colors. The home screen should look good in both light and dark mode -- verify both.
  </action>
  <verify>
Run `npm run dev`:
1. Home screen shows two large side-by-side cards for Builder and Player
2. Each card has: icon (teal color), title (bold), description (muted)
3. Cards have hover effects (border highlight, shadow, subtle scale)
4. Below the cards: "No recent files" empty state message
5. Click Builder card -- navigates to Builder placeholder, titlebar shows home button
6. Click home button -- returns to home screen
7. Click Player card -- navigates to Player placeholder
8. Toggle Windows dark mode -- entire UI switches theme (backgrounds, text, card colors all update)
9. Layout looks good on default window size AND maximized window (cards stay centered, don't stretch too wide)
10. `npm run build` still succeeds
  </verify>
  <done>
Home screen displays two side-by-side mode cards (Builder with Wrench icon, Player with Play icon) with teal accent, hover effects, and click-to-navigate. Recent files section shows empty state. Builder and Player routes show clean placeholders. Full navigation flow works: home -> builder/player -> home (via titlebar). Both light and dark themes render correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 app shell: Electron app with custom frameless titlebar, home screen with Builder/Player mode cards, HashRouter navigation, CSS variable theming with teal accent, and system light/dark mode detection.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` from D:\Code\PromptPlayer
2. Verify the app window appears with NO native Windows titlebar (custom chrome only)
3. Verify the titlebar shows "PromptPlayer" text on the left and min/max/close buttons on the right
4. Drag the titlebar to move the window
5. Click minimize -- window minimizes to taskbar
6. Click maximize -- window maximizes, icon changes to restore
7. Click restore -- window returns to previous size
8. Verify home screen shows two large side-by-side cards: "Builder" (wrench icon) and "Player" (play icon)
9. Verify each card has a one-line description below the title
10. Verify "No recent files" appears below the cards
11. Click the Builder card -- should navigate to Builder placeholder
12. Verify home button appears in the titlebar -- click it to return home
13. Click the Player card -- should navigate to Player placeholder
14. Resize the window -- layout should adapt (cards stay centered)
15. Open Windows Settings > Personalization > Colors > toggle dark/light mode
16. Verify the app theme updates to match (backgrounds, text, card colors all switch)
17. Verify teal accent is visible on card hover and any accent elements
18. Close and reopen the app -- verify it remembers its last window size and position
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 1 success criteria (from ROADMAP.md):
1. App launches on Windows 10/11 without errors -- verified by `npm run dev`
2. User sees a home screen with clear options to enter Builder mode or Player mode -- two side-by-side cards
3. Selecting Builder or Player navigates to the respective mode -- click cards, see placeholder content
4. App window is resizable and has standard window controls -- custom titlebar with min/max/close

Requirements:
- SHELL-01: Electron app runs on Windows 10/11 -- verified
- SHELL-02: App opens to a home screen where user can choose Builder or Player mode -- verified
</verification>

<success_criteria>
- Home screen shows two side-by-side cards (Builder with Wrench icon, Player with Play icon)
- Each card has icon, title, and one-line description per locked decision
- Recent files section below cards shows empty state
- Cards are clickable and navigate to respective mode placeholders
- Custom titlebar with "PromptPlayer" brand, home button, and window controls
- Home button appears only when not on home route
- Full light/dark theme switching via system detection
- Teal accent color visible throughout
- Window state (size, position) persists between launches
- Clean build with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell/01-02-SUMMARY.md`
</output>
