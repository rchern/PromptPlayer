---
phase: 03-message-rendering
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/renderer/src/routes/Builder.tsx
autonomous: false

must_haves:
  truths:
    - "User can see a rendered conversation preview when selecting a session in Builder"
    - "User messages and Claude responses are visually distinct in the preview"
    - "Plumbing tool calls do not appear in the rendered preview"
    - "Text is readable at screen-sharing distance (large base font, high contrast)"
    - "Code blocks have syntax highlighting with language badges"
  artifacts:
    - path: "src/renderer/src/routes/Builder.tsx"
      provides: "Builder page with MessageList integrated in session detail panel"
      contains: "MessageList"
  key_links:
    - from: "src/renderer/src/routes/Builder.tsx"
      to: "src/renderer/src/components/message/MessageList.tsx"
      via: "import and render with activeSession.messages"
      pattern: "MessageList.*messages.*activeSession"
---

<objective>
Wire the MessageList component into the Builder's session detail panel so that selecting a session shows a rendered conversation preview. Then verify the full rendering pipeline visually.

Purpose: This connects Phase 2's data pipeline to Phase 3's rendering components, making the conversation viewable for the first time. Visual verification confirms all five success criteria are met.
Output: Working conversation preview in Builder mode. Visual confirmation of rendering quality.
</objective>

<execution_context>
@C:\Users\Rebecca\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Rebecca\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-message-rendering/03-RESEARCH.md
@.planning/phases/03-message-rendering/03-01-SUMMARY.md
@.planning/phases/03-message-rendering/03-02-SUMMARY.md
@src/renderer/src/routes/Builder.tsx
@src/renderer/src/stores/sessionStore.ts
@src/renderer/src/types/pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate MessageList into Builder session detail panel</name>
  <files>
    src/renderer/src/routes/Builder.tsx
  </files>
  <action>
1. In `src/renderer/src/routes/Builder.tsx`, import MessageList:
   ```typescript
   import { MessageList } from '../components/message'
   ```

2. In the session detail panel (the section that shows when `activeSession && !isParsing`), replace or augment the existing stat cards with a conversation preview:
   - Keep the Session ID display and the 3 stat cards (Messages, Orphaned, Sidechains) at the top for context
   - Below the stats, add a section header "Conversation Preview" with styling: font-size var(--text-sm), font-weight 600, color var(--color-text-muted), text-transform uppercase, letter-spacing 0.05em, margin-top var(--space-4)
   - Below the header, render `<MessageList messages={activeSession.messages} />` (showPlumbing defaults to false)
   - The MessageList container should take the remaining vertical space. Apply flex: 1 and min-height: 0 to allow it to scroll independently within the detail panel.
   - The detail panel itself already has overflowY: auto -- but the MessageList should manage its own scroll. Change the detail panel to use flex layout (flex-direction: column) and remove its overflowY: auto. Give the MessageList wrapper div flex: 1, overflow-y: auto, min-height: 0.

3. Verify the detail panel layout works:
   - Session ID + stats at top (fixed height, no scroll)
   - "Conversation Preview" label
   - MessageList filling remaining space with its own scroll
  </action>
  <verify>
    Run `npx tsc --noEmit -p tsconfig.web.json --composite false` -- no type errors.
    Run `npm run build` -- build succeeds.
    Run `npm run dev` to launch the app and visually confirm the rendering works.
  </verify>
  <done>
    Builder session detail panel shows a rendered conversation preview with MessageList when a session is selected. Stats remain visible at top, conversation scrolls below.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 3 success criteria</name>
  <what-built>
    Complete message rendering pipeline: markdown with GFM, syntax-highlighted code blocks, tool call visibility filtering, thinking block toggles, presentation typography, and user/Claude visual distinction. Integrated into Builder session detail panel as conversation preview.
  </what-built>
  <how-to-verify>
    1. Launch the app with `npm run dev`
    2. Navigate to Builder mode
    3. Select a session that has varied content (markdown, code blocks, tool calls, thinking blocks)

    **Check each criterion:**

    a) **Visual distinction (PLAY-04):** User messages have tinted background with "You" label. Claude messages have default background with "Claude" label. The two roles are immediately distinguishable.

    b) **Markdown rendering (PLAY-05):** Claude's responses show proper headings (different sizes), bullet lists, bold text, italic text, links (colored), and tables (if present in the session). Text is not raw markdown.

    c) **Syntax highlighting (PLAY-06):** Code blocks have colored syntax highlighting. Language badge appears in top-right corner (e.g., "TYPESCRIPT", "PYTHON"). Code blocks with many lines have a max-height with scroll.

    d) **Plumbing hidden (PLAY-09):** Read, Grep, Glob, Write, Edit, Bash tool calls do NOT appear in the preview. If a session has narrative tools (AskUserQuestion, TaskCreate), those DO appear with tool name and summary.

    e) **Readability (PLAY-15):** Text is noticeably larger than typical IDE/terminal text. Content is centered with comfortable line length (~60-75 chars). Spacing between messages is generous. Would be readable at 10+ feet on a projector.

    f) **Thinking blocks:** If present, they show as collapsed "Thinking..." text with a toggle. Clicking expands to show the thinking content.

    g) **Dark mode:** Toggle to dark mode (if available). Code blocks switch to dark theme. All text remains readable.

    4. If any criterion fails, describe the issue for fix.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. Builder session detail panel renders MessageList when a session is selected
2. All five Phase 3 success criteria visually verified:
   - PLAY-04: User/Claude visual distinction
   - PLAY-05: Markdown formatting (headings, lists, bold, italic, links, tables)
   - PLAY-06: Syntax highlighting with language badges
   - PLAY-09: Plumbing tool calls hidden
   - PLAY-15: Presentation-quality typography
3. TypeScript compiles, build succeeds
</verification>

<success_criteria>
- Selecting a session in Builder shows a rendered conversation preview
- All five requirements (PLAY-04, PLAY-05, PLAY-06, PLAY-09, PLAY-15) are visually confirmed
- The rendering looks professional and presentation-ready
- Human verifier approves the visual output
</success_criteria>

<output>
After completion, create `.planning/phases/03-message-rendering/03-03-SUMMARY.md`
</output>
