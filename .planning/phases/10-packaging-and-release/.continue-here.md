---
phase: 10-packaging-and-release
task: CI lint fix
total_tasks: N/A (post-execution UAT fix)
status: in_progress
last_updated: 2026-03-02T05:34:06.766Z
---

<current_state>
Phase 10 execution complete (3/3 plans). UAT passed 9/9 after focus fix. But CI workflow failed on first PR because ESLint was never configured — no `eslint.config.mjs` existed. Created the config file but lint reports 3251 errors across the entire codebase. The eslint config commit is local, not yet pushed.
</current_state>

<completed_work>

- Phase 10 execution: all 3 plans complete (10-01 build infra, 10-02 single-instance + file opening, 10-03 CI/CD)
- UAT: 9/9 tests passed (installer, wizard, app launch, identity, file association, cold/warm start, syntax highlighting, uninstall)
- Focus fix: `setAlwaysOnTop` workaround for Windows focus-stealing prevention (commit 04c8669)
- GitHub owner: replaced OWNER_PLACEHOLDER with `rchern` in electron-builder.yml (commit af29b1e)
- ESLint config: created `eslint.config.mjs` importing `@electron-toolkit/eslint-config-ts` recommended (commit 725f879)
</completed_work>

<remaining_work>

- **ESLint config needs ignores**: `out/`, `dist/`, `scripts/`, `node_modules/` should be excluded
- **3251 lint errors to resolve**: Mostly `explicit-function-return-type` and `no-unused-vars`. Options:
  1. Relax strict rules in config (quick — disable `explicit-function-return-type`)
  2. Fix all errors (thorough but large diff)
  3. Hybrid: disable the noisiest rules, fix genuine issues
- **Push fix and re-run CI**: After lint passes locally, push to branch and verify CI goes green
- **Phase 10 completion**: Once CI passes, phase is truly done
- **Developer Mode note**: Document that Windows Developer Mode is required to build locally (winCodeSign symlink issue)
</remaining_work>

<decisions_made>

- NSIS assisted installer (oneClick: false) — user chose for desktop shortcut opt-out (though opt-out checkbox wasn't surfaced, noted as polish)
- Draft GitHub Releases (releaseType: draft) — review before publish
- setAlwaysOnTop toggle for Windows focus workaround
- Developer Mode required for local builds on Windows (symlink privilege for winCodeSign extraction)
- Phase 10 verifier was overridden to opus per MEMORY.md guidance
</decisions_made>

<blockers>
- CI fails on lint — must fix eslint config + errors before PR can merge
</blockers>

<context>
The CI workflow itself is correctly structured (lint + typecheck + build on PRs to main). The failure exposed that eslint was never configured in the project — electron-vite scaffold likely expected the user to set it up. The `@electron-toolkit/eslint-config-ts` package was already installed as a devDependency but never wired. The `npm run lint` script used the old `--ext` flag syntax which is also ESLint 8 style (flat config ignores it, but it still works). The recommended approach is probably to relax `explicit-function-return-type` since the codebase was built without it, and fix genuine errors (unused vars, useless escapes). Also need to add ignores for build output directories.

UAT polish items noted but not blocking:
- No desktop shortcut opt-out in installer
- 5-10s delay on warm-start file open
- Shiki syntax highlighting loading delay
</context>

<next_action>
Start with: Fix eslint.config.mjs — add ignores for out/, dist/, scripts/, node_modules/. Then either disable `explicit-function-return-type` rule or set to warn. Run `npm run lint` locally. Fix remaining real errors. Push and verify CI passes. Then phase 10 is truly complete.
</next_action>
