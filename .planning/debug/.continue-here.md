---
phase: bugfix-askuserquestion-and-player-ux
task: 5
total_tasks: 6
status: in_progress
last_updated: "2026-03-11T03:56:46.150Z"
---

<current_state>
Debugging AskUserQuestion display issues and Player UX improvements using test file "D:\Users\Rebecca\Downloads\Email MFA with GSD.promptplay". All fixes compile clean (tsc --noEmit passes). User has verified most fixes visually. Loading spinner feature code-complete but not yet tested by user.
</current_state>

<completed_work>

- Task 1: Combined steps not passing followUpMessages — FIXED
  - `StepView.tsx:116`: Changed `idx === 0 ? step.followUpMessages : []` to `step.followUpMessages`
  - Root cause: only first combined message got answer data; all others got empty array

- Task 2: Tool rejections not showing as user steps — FIXED
  - `messageFiltering.ts`: Added `isRejection()` helper; `isToolResultOnly` returns false for rejections
  - Rejections now create visible "YOU" steps AND copy into previous step's followUpMessages
  - `StepView.tsx`: Suppresses rejection followUps from bottom rendering (they have their own step)
  - `AskUserQuestionBlock.tsx`: Shows inline "Declined" indicator with red styling

- Task 3: Comma-in-answer breaking option matching — FIXED
  - `AskUserQuestionBlock.tsx`: Smart comma-split — tries split first, falls back to full string if no options match
  - Fixes "Yes, store it" and "Yes, retain selection" showing as custom answers

- Task 4: Duplicate free-text answer + user notes — FIXED
  - `cleanUserText.ts`: Strip trailing `"` (protocol quote) in `extractNotes()`
  - `AskUserQuestionBlock.tsx`: Suppress notes when identical to a free-text answer

- Task 5: Loading spinner during slow markdown renders — CODE COMPLETE, needs visual verification
  - `PlaybackPlayer.tsx`: Added loading state with double-RAF paint detection, Loader2 spinner overlay
  - `usePlaybackKeyboardNavigation.ts`: Accepts loadingRef to block navigation during rendering

</completed_work>

<remaining_work>

- Task 5: User needs to verify the loading spinner visually (restart npm run dev)
- Task 6: Embedded quotes parser improvement (agent's cleanUserText.ts changes) — included but was not the primary fix. The `"="`-split parser is more robust than the old regex. Already tested: 127/129 answers parse correctly vs 115/129 before.
- Commit all changes
- Update debug session file `.planning/debug/askuserquestion-selected-answers.md` to resolved status
- Consider updating MEMORY.md with new decisions

</remaining_work>

<decisions_made>

- Rejections show in BOTH places: inline "Declined" on the question block AND as a visible "YOU" step on the next slide (user's requested compromise)
- Comma-split uses "try split, fallback to full string" strategy rather than a smarter delimiter — simple and handles the common cases
- Loading spinner uses double-requestAnimationFrame to detect paint completion — standard React pattern
- Loading gate uses a ref (not state) so the keyboard event handler doesn't need to re-register on every loading change
- Parser uses `"="` protocol delimiter split instead of regex — handles embedded quotes in question/answer text

</decisions_made>

<blockers>
None — all fixes compile clean and most are visually verified.
</blockers>

<context>
The test file is "D:\Users\Rebecca\Downloads\Email MFA with GSD.promptplay" — a large presentation with 530+ steps, heavy use of AskUserQuestion across multiple sessions. The Phase 8 "Admin Creation and Access Control Domain" section is the best area to verify fixes (slides 170-200 range). The early slides (1-10 "New Milestone") have the rejection/decline test cases.

There are also 4 older unresolved debug sessions in .planning/debug/ (blank-combined-steps, close-button-flicker, empty-recent-files, live-preview-reactivity) — not addressed this session.
</context>

<next_action>
1. Have user verify loading spinner works (npm run dev, navigate to a heavy slide)
2. Commit all 6 changed files
3. Optionally mark askuserquestion-selected-answers debug session as resolved
</next_action>
